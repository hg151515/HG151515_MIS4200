@model IEnumerable<HG151515_MIS4200.Models.CourseDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Instructor.instructorfirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.coursedetailDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.coursestartTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.courseID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Instructor.instructorfirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coursedetailDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coursestartTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.courseID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.coursedetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.coursedetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.coursedetailID })
        </td>
    </tr>
}

</table>
